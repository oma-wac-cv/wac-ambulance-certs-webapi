 apiVersion: v1
 kind: PersistentVolumeClaim
 metadata:
   name: mongo-pvc-oma-certs
 spec:
   # storageClassName: default
   accessModes:
     - ReadWriteOnce
   resources:
     requests:
       storage: 1Gi
